services:
  - type: web
    name: redishelper
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/ws-proxy.js & vite
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
    websocketSupport: true