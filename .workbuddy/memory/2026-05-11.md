# 2026-05-11 工作日志

## 实现 keys 不区分大小写搜索

**文件**: `src/frontend/views/MainView.vue`

**方案**: 在 `sortedKeys → keyTree` 之间插入 `filteredKeys` computed，实时响应 `searchPattern` 变化做客户端 case-insensitive 过滤。

**改动**:
1. 新增 `filteredKeys` computed：`key.toLowerCase().includes(pattern.toLowerCase())`
2. `keyTree` 改为使用 `filteredKeys` 而非 `sortedKeys`
3. 重构 `searchKeys`：普通关键词 → 纯客户端过滤（无需后端调用）；含 Redis 通配符 `* ? [` → 走后端 + 客户端二次过滤
4. 搜索框 placeholder 改为"搜索 key（不区分大小写）"
5. 顺手修复两处 `inputName` 未使用的 TS6133 错误（清空DB/删除DB的 ElMessageBox.prompt）

**构建结果**: ✓ 0 错误
