app = "redis-helper"
primary_region = "sin"

[build]
dockerfile = "Dockerfile"

[[services]]
  protocol = "tcp"
  internal_port = 10000

  [[services.ports]]
    port = 10000

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1